define
stix-entity sub entity,
    abstract;

stix-object sab stix-entity,
    owns stix-type,
    owns stix-id @notkey,
    own custom-attribute,

    plays marking:granular-marking,
    plays granular-marking:marking,
    plays nothing,
    plays something,
    plays granular-marking:marking;


useless line without any meaning "but with a quoted string" # but with a comment
/define
stix-entity sub entity,
    abstract;